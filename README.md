# expo-backend

Backend API для работы с клиентами, использует PostgreSQL (Supabase) и Express.

## 📝 Требования

- Node.js >= 18
- Yarn 1.x
- При изменении `.env` файла, нужно также обновлять соответствующую переменную в Railway внутри проекта `expo-backend`.

Обязательные переменные `.env`:

- `DATABASE_URL` — строка подключения к базе Supabase  
- `PORT` — порт для сервера (по умолчанию 8080)

## ⚡ Установка

```bash yarn install ```

## 🛠 Скрипты

### 📦 Сборка проекта
```bash yarn build ```

### 💻 Режим разработки
```bash yarn dev ```

### 🚀 Запуск в продакшн

```bash yarn start ```

### 🧹 Lint (проверка кода с Biome)
```bash yarn lint ```

### 🧹 Lint с автозаправкой (фиксирование кода)
```bash yarn lint:fix ```

### 📦 Генерация Prisma Client
```bash yarn prisma:generate ```

### 🗂 Создать дамп базы данных приложения
```bash yarn dump:db:app ```

### 🗂 Создать дамп системных схем базы данных
```bash yarn dump:db:system ```

### 🗂 Собрать все исходники в один файл (src_all.txt)
```bash yarn dump:files ```

### 🔌 Проверить подключение к базе данных
```bash yarn check:db ```
